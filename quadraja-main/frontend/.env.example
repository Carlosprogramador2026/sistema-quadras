# Em dev local, deixe vazio: o Vite faz proxy de /api para o backend (porta 3333).
# Em producao (deploy no Vercel/Netlify), aponte para a URL publica do backend:
# VITE_API_URL=https://quadra-backend.onrender.com/api
VITE_API_URL=
